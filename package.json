{
  "dependencies": {
    "@neondatabase/serverless": "^0.9.4",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "vitest": "^2.0.5",
    "wrangler": "^3.72.0"
  },
  "name": "developer-friendly-newsletter",
  "private": true,
  "scripts": {
    "deploy": "wrangler publish",
    "lint": "eslint . --fix --ignore-pattern *.wrangler/*",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "type": "module",
  "version": "0.0.0"
}
